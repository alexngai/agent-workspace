/**
 * @deprecated Import from `agent-workspace/protocol/task` (subpath) or
 *   `agent-workspace/protocol` (umbrella) instead. This module is kept
 *   as a back-compat re-export so existing imports keep working; it will
 *   be removed in v1.0. See `docs/design/architecture.md` decision D7.
 */
export * from '../protocol/task.js';
